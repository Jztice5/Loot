# Loot 项目代码规范补充

## Python 注释与 docstring 规则

Loot 的 Python 代码以 `code-standards` Java 版业务 Javadoc 思路为注释基准，
但表达形式使用 Python docstring。

核心契约类、状态机、Policy Gate、Skill Runtime、Provider 和 Alert 组件必须包含：

- `业务描述`：说明这个类从业务角度负责什么。
- `业务场景`：说明在哪些流程中会使用。
- `业务原因`：说明为什么需要这个边界或约束。
- `调用链`：用 `A -> B -> C` 标明主要流向；事件或异步流可写 `A -> Event -> Consumer`。
- `业务规则`：列出关键状态、幂等、权限、时间、不可变性或禁止事项。

方法级注释遵循分层策略：

- 简单字段校验器只写技术说明或不额外扩写。
- 核心 validator 和状态迁移方法必须写清楚业务原因。
- 分支判断前优先写“为什么这样判断”，避免把代码翻译成中文。
- 易踩坑处使用 `注意:` 标明，例如幂等键、UTC 时间、状态机准入顺序。

禁止事项：

- 不写空洞模板，例如“处理业务逻辑”。
- 不在方法级重复类级已经说明的通用背景。
- 不写与代码不同步的历史说明。
- 不用注释替代清晰命名。
