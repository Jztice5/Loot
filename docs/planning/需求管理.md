# Loot 需求管理

> 这里记录阶段目标、需求项、验收标准和推进顺序。
> 日常流水计划仍放在 [开发计划.md](开发计划.md)，稳定设计放到 architecture。

## 当前阶段

| 字段 | 内容 |
|---|---|
| 阶段 | Phase 0：Architecture Foundation |
| 当前焦点 | Crypto K 线地基完成后，准备进入 Candidate 最小闭环 |
| 当前分支 | `codex/project-review-20260710` |
| 最近稳定提交 | `dd3d943 refactor(loot): 优化 Crypto K 线地基语义` |
| 验证基线 | `30 tests OK`，OKX public REST smoke 通过 |

## 已完成需求

### REQ-0001：项目上下文分层

状态：Done

目标：

- 建立适合长期 vibe coding 的文档分层。
- 让后续 Codex 可以从入口文档快速恢复项目状态。

交付：

- `docs/README.md`
- `docs/development/memory.md`
- `docs/architecture/README.md`
- `docs/runbooks/context-health-check.md`

### REQ-0002：核心契约骨架

状态：Done

目标：

- 建立跨模块强类型契约。
- 固定不可变、禁止额外字段、UTC 时间等基础规则。

交付：

- `src/loot/contracts`
- `docs/architecture/contracts/loot-contracts-v0.1.md`

### REQ-0003：Signal State Machine v0.1

状态：Done

目标：

- 建立 Signal 状态事实的唯一写入入口。
- 验证合法迁移、非法迁移、同票据幂等和 no-op。

交付：

- `src/loot/signals/state_machine.py`
- `docs/architecture/signal-state-machine/loot-signal-state-machine-v0.1.md`

### REQ-0004：Crypto K 线行情地基

状态：Done

目标：

- 接入 Crypto 只读 K 线数据源。
- 标准化 OKX public REST 原始 K 线。
- 默认只返回已收盘 K 线，避免后续策略误用盘中噪声。

交付：

- `MarketBar`
- `MarketSnapshot`
- `MarketBarClosedEvent`
- `FakeCryptoProvider`
- `OkxRestCryptoProvider`
- [Crypto 行情地基评审](../reviews/crypto-market-data-foundation-review-2026-07-10.md)

## 下一环节需求

### REQ-0005：Crypto Candidate PreFilter 最小版本

状态：Next

目标：

- 从 `MarketSnapshot.latest_closed_bar` 生成第一条可复现 `CandidateEvent`。
- 先跑通确定性预筛选，不接 Agent，不接 LLM。
- 验证重复行情不会重复产生候选。

范围：

- 只处理 Crypto。
- 只使用已收盘 K 线。
- 优先支持一个简单、可解释的候选类型，例如 `STRUCTURE_BREAKOUT` 或
  `PRICE_ZONE_APPROACH`。
- 使用 `FakeCryptoProvider` 作为默认测试输入。

非目标：

- 不接真实交易提醒。
- 不接 Policy Gate。
- 不接 Agent 或 Skill Runtime。
- 不扩展 funding、OI、order book、ticker 等数据类型。

建议交付：

- `src/loot/domains/crypto/prefilter.py`
- `CryptoPreFilterInput` 或轻量方法参数，优先复用现有契约。
- 生成 `CandidateEvent` 的确定性规则。
- `tests/unit/domains/crypto/test_prefilter.py`
- `docs/architecture/market-domains/crypto-prefilter-v0.1.md`

验收标准：

- 无候选条件时返回空结果或明确的 no-candidate 结果。
- 满足条件时生成一个 `CandidateEvent`。
- `CandidateEvent.snapshot_id` 指向 `MarketSnapshot.id`。
- `dedupe_key` 对同一标的、周期、候选类型和 K 线稳定。
- 未收盘 K 线不能触发候选。
- 重复输入不会产生不同的 dedupe_key。
- 单元测试覆盖成功、无候选、未收盘过滤、重复输入。

### REQ-0006：Crypto Golden Case 最小集

状态：Planned

目标：

- 为第一条 Crypto 闭环准备可重复回放样例。
- 验证行情输入、候选生成和后续状态机消费的稳定性。

范围：

- 先用 FakeProvider 构造 2 到 3 个样例。
- 不引入真实历史行情归档。

建议 Golden Case：

1. 普通上涨但未触发候选。
2. 收盘突破阈值，生成 `STRUCTURE_BREAKOUT`。
3. 未收盘突破但收盘前回落，不生成候选。

验收标准：

- Golden Case 输入可读、可复现。
- 每个样例有明确预期输出。
- 后续可被 replay 测试复用。

### REQ-0007：Candidate 到 Signal 的最小串联

状态：Planned

目标：

- 将 Fake Candidate 转成最小 `DecisionTicket`，调用 Signal State Machine。
- 验证端到端幂等语义。

范围：

- 暂时不接 Agent。
- 暂时用 deterministic decision builder。
- 只验证 `OBSERVING -> ARMED` 或 `ARMED -> TRIGGERED` 一条合法迁移。

验收标准：

- 同一 Candidate 重复消费不会产生重复 SignalEvent。
- 非法迁移被拒绝。
- 状态变化有稳定 `decision_ticket_id` 和 `input_snapshot_id`。

## 后续暂缓需求

| 需求 | 暂缓原因 |
|---|---|
| OKX WebSocket | 先验证 REST + 已收盘 K 线闭环 |
| funding / OI / liquidation | 等 K 线闭环稳定后按策略需要新增契约 |
| Agent / Skill Runtime | 需要 Candidate 和 Golden Case 先稳定 |
| Alert Center | 需要 SignalEvent 端到端稳定后再接 |
| 持久化和 migrations | 需要先固定核心幂等键和事件语义 |

## 下一次开工建议顺序

1. 阅读 [Crypto 行情地基评审](../reviews/crypto-market-data-foundation-review-2026-07-10.md)。
2. 阅读 [Crypto 行情 Provider 设计](../architecture/market-domains/crypto-market-data-provider-v0.1.md)。
3. 实现 REQ-0005，不推进 REQ-0007。
4. 用 PyCharm Terminal 跑 `unittest` 和 `compileall`。
5. 更新 memory、过程日志和需求状态。
