# 开发计划

> 每天简单记录：今天做什么、做完了什么、明天接着做什么。

## 2026-07-10

### 今天做什么

- 对齐 Loot 的设计文档分层，参考智能客服项目的文档组织方式。
- 把宏观设计和自选持仓信号监控闭环设计拆到 architecture 下的不同层级。
- 建立 development memory、过程日志模板、评审记录和 runbook 入口。

### 今天完成

- 创建 `docs/README.md` 作为统一文档入口。
- 将宏观架构移动到 `docs/architecture/system/`。
- 将自选与持仓信号监控闭环移动到 `docs/architecture/signal-monitoring/`。
- 新增 `docs/development/memory.md`、过程日志索引、过程日志模板、评审备忘和本地运行 runbook。
- 补齐上下文维护规则：过程日志、健康检查 runbook、AGENTS 收尾归档动作。
- 创建全局 Codex skill `vibe-context-manager`，用于复用这套上下文管理机制。
- 建立 Python 项目骨架：`pyproject.toml`、`src/loot`、`tests`。
- 定义第一批核心 contracts，并补契约设计文档和单元测试。
- 实现 Signal State Machine v0.1，覆盖合法迁移、非法迁移、幂等和 no-op。
- 接入 Crypto 行情数据 Provider v0.1，包含 MarketBar/MarketSnapshot 契约、
  FakeCryptoProvider 和 OKX public REST K 线。

### 今日收尾

- Review 并优化 Crypto K 线地基语义：`MarketSnapshot.bars` 改为 tuple，
  OKX Provider 默认只返回已收盘 K 线。
- 新增 [Crypto 行情地基评审](../reviews/crypto-market-data-foundation-review-2026-07-10.md)。
- 新增 [需求管理](需求管理.md)，把下一环节拆为 REQ-0005 到 REQ-0007。

### 下一环节做什么

- 做 `REQ-0005`：Crypto Candidate PreFilter 最小版本。
- 做 `REQ-0006`：Crypto Golden Case 最小集。
- 之后再做 `REQ-0007`：Candidate 到 Signal 的最小串联。
- 持久化层中的 SignalTransition、DecisionTicket 消费标记和 outbox 暂不抢跑。

### 备注

- 当前仓库已有第一批 contracts、Signal State Machine v0.1 和 Crypto 行情 Provider v0.1 代码，但 `main.py` 仍是示例脚本。
- 下一环节需求拆分以 [需求管理.md](需求管理.md) 为准。
- 后续每轮实质变更结束时，按 `docs/runbooks/context-health-check.md` 做一次上下文健康检查。

## 2026-07-14

### 今天完成

- 回归 Candidate 到 Signal 的端到端设计。
- 将 Policy 前的决策建议明确为 `DecisionProposal`。
- 明确 Policy Gate 必须记录 `PolicyEvaluation`，且只有批准时才能签发
  `DecisionTicket`。
- 补充决策链路的事务、幂等、失败、追踪和 Position 边界。
- 调整开发顺序：Golden Case 先于 PreFilter 实现。
- 完成第二轮复核并补齐 Snapshot 内容寻址、闭合时间、Policy 可重评、Ticket
  上下文绑定、Signal generation 和投影完整校验约束。
- 完成 `REQ-0009` 第二轮地基不变量修正：Snapshot 使用完整窗口内容寻址，拒绝
  提前闭合 K 线，Signal 支持初始空 Ticket，状态机恢复完整投影校验和 payload 指纹。
- 新增 11 个回归测试，完整基线从 30 增至 41；`compileall`、行长和 diff 检查通过。

### 下一环节做什么

1. `REQ-0006`：定义 Crypto Golden Case 输入和预期。
2. `REQ-0005`：按 Golden Case 实现 Crypto Candidate PreFilter。
3. `REQ-0007`：完成 Proposal、Policy、Ticket 和 State Machine 最小串联。
4. `REQ-0008`：再把内存幂等升级为持久化事务语义。

### 备注

- 当前 Python `DecisionTicket` 仍表达 Policy 前建议，属于已登记的契约迁移项。
- 在 `REQ-0007` 前，不新增直接创建旧 DecisionTicket 的生产代码。

## 待办

- REQ-0006：Crypto Golden Case 最小集。
- REQ-0005：Crypto Candidate PreFilter 最小版本。
- REQ-0007：Candidate 到 Proposal、Policy、Ticket、Signal 的最小串联。
- REQ-0008：决策链路持久化可靠性基线。
